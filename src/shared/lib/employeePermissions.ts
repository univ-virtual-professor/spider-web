// Re-exported from rolesConfig — import directly from there for new code
export {
  PERMISSIONS,
  PERMISSION_LABELS,
  PERMISSION_GROUPS,
  DEFAULT_ROLES,
  type Permission,
  type RoleTemplate,
} from "./rolesConfig";
